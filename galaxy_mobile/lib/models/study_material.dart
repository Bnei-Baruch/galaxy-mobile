// TODO: move to JsonSerializable once we upgrade flutter to null safety.
class StudyMaterial {
  // String userName;
  // String roomName;
  // String content;
  // bool askForMe;
  // int timestamp;
  //
  // StudyMaterial({this.userName, this.roomName, this.content, this.askForMe, this.timestamp});
  //
  // factory StudyMaterial.fromJson(Map<String, dynamic> json) {
  //   return StudyMaterial(
  //       userName: json['user']['name'] ?? '',
  //       roomName: json['user']['galaxyRoom'] ?? '',
  //       content: json['question']['content'] ?? '',
  //       askForMe: json['askForMe'] ?? false,
  //       timestamp: json['timestamp'] ?? 0);
  // }
}